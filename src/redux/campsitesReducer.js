import { CAMPSITES } from '../shared/campsites';   

/* this is the reducer function to handle this part of the state */
export const CampsitesReducer = (state = CAMPSITES, action) => {
    switch (action.type) {
        default: return state; 
    }       
}